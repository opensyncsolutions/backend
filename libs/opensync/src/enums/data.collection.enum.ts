export enum SURVEYSTATUS {
  complete = 'COMPLETE',
  overdue = 'OVERDUE',
  ready = 'READY',
  notReady = 'NOT READY',
  received = 'RECEIVED',
}

export enum SAMPLESTATUS {
  complete = 'COMPLETE',
  overdue = 'OVERDUE',
  ready = 'READY',
  notReady = 'NOT READY',
  received = 'RECEIVED',
  failed = 'FAILED',
  invalid = 'INVALID',
  rejected = 'REJECTED',
  unreviewed = 'UNREVIEWED',
  '-' = '-',
}
