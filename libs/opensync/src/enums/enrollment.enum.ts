export enum GENDER {
  male = 'Male',
  female = 'Female',
}

export enum ENROLLMENTSTATUS {
  control = 'CONTROL',
  intervention = 'INTERVENTION',
}

export enum SESSIONNUMBER {
  first = 1,
  second = 2,
  third = 3,
  fourth = 4,
}

export enum FIELDTYPE {
  date = 'DATE',
  text = 'TEXT',
  long_text = 'LONG_TEXT',
  bool = 'BOOLEAN',
  num = 'NUMBER',
}
